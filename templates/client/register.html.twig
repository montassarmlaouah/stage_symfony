<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion  Client- Clic IT Shop</title>
    <link rel="stylesheet" href="{{ asset('css/style-from.css') }}">
</head>
<body>
    <div class="main-container">
        <div class="left-section">
            <h1>BIENVENUE DANS <span>Clic IT Shop</span></h1>
            
    
            <img src="{{ asset('images/clientwelecom-image.png') }}" alt="Welcome">
        </div>

        <div class="right-section">
            <div class="logo">
                <img src="{{ asset('images/logo1.png') }}" alt="Logo" />
            </div>
            <div class="container">
                <h1>Inscription</h1>
                {{ form_start(registrationForm) }}
                {{ form_widget(registrationForm) }}
                <button type="submit" class="btn btn-primary">S'inscrire</button>
                {{ form_end(registrationForm) }}
                <p class="footer-text">
                    Vous avez déjà un compte ? <a href="{{ path('client_login') }}">Se connecter</a>
                </p>
            </div>
        </div>
    </div>
</body>
</html>