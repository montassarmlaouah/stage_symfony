{% extends 'base.html.twig' %}

{% block body %}
<h1>{{ form.vars.value.id ? 'Edit Article' : 'Create New Article' }}</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Save</button>
{{ form_end(form) }}
<a href="{{ path('article_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}
