<form method="post" action="{{ path('info_delete', { id: info.id }) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette inforamtion ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>